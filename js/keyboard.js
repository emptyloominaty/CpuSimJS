window.addEventListener("keydown", function (event) {

    /*switch (event.key) {
        case "ArrowDown":
            pressKey(1)
            break
        case "ArrowUp":
            pressKey(2)
            break
        case "ArrowLeft":
            pressKey(3)
            break
        case "ArrowRight":
            pressKey(4)
            break
        default:
            return
    }*/

    pressKey(event.keyCode)

}, true);
